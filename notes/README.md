# Working notes

**These are not documentation.** They are kept for provenance — so that a decision can be
traced to the moment it was made — and they are not maintained against the code.

They lived in `docs/internal/` until 2026-08-16 ([issue #41](https://github.com/comeni-project/Comeni-Labs/issues/41)):
69 of the 94 markdown files under `docs/` were these, so `ls docs/` showed the working record
before it showed anything a reader wanted. Moving them out is the whole change — nothing was
archived, deleted or rewritten.

`make links` deliberately does **not** check this directory. A plan naming a file its own tasks
create is correct at the moment it executes and broken until then, so checking the record would
make `make check` red for the duration of every plan.

If you want to know how Mendel works, read [`ARCHITECTURE.md`](../ARCHITECTURE.md) and
[`docs/`](../docs/README.md). If you want to know *why*, read [`docs/design/`](../docs/design/).

## journal/

**Start here if you are picking the project up.** One append-only entry per working
session: where things stand, what changed, what was decided and rejected, what is next, and
what a fresh reader gets wrong. Newest first — see [journal/README.md](journal/README.md).

## plans/

Step-by-step implementation plans, written before the work and executed task by task.
They contain code that was proposed, not necessarily code that shipped — several steps in
every plan were corrected during execution, and the corrections are recorded in the commit
messages rather than back-ported here.

**Filenames are a log, not an order** — two plans share the date `2026-08-04`. This table is
the order.

| Order | Plan | Status |
|---|---|---|
| 1 | `2026-08-02-mendel-deterministic-spine.md` | Plan 1 — complete |
| 2 | `2026-08-03-measurements-rules-and-profiling.md` | complete |
| 3 | `2026-08-04-the-runnable-spine.md` | Plan 1.5 — **complete** |
| 4 | `2026-08-05-conformance-checking.md` | Plan 1.6 — **complete** |
| 5 | `2026-08-04-publication-and-the-registry-split.md` | Plan 1.7 — **complete** |
| 6 | `2026-08-06-closing-the-audit.md` | Plan 1.8 — **complete.** Closed A1–A13 and A15; A14 and A16 stay open |
| 7 | `2026-08-07-closing-round-two.md` | Plan 1.9 — **complete.** Closed A17–A35; A14, A36 open, A37 fixed |
| 8 | `2026-08-07-the-pipeline-file.md` | Plan 1.10 — `pipeline.yml`. **Complete**, with each task's corrections inline |
| 9 | `../audits/2026-08-10-round-three-audit.md` | Round three audit — **complete.** A38–A54, four critical (A38, A44, A46, A50) |
| 10 | `2026-08-10-closing-round-three.md` | Plan 1.11 — **complete.** Closed A38–A54 (17 tasks), with each task's corrections inline; A14 stays open |
| 11 | `../audits/2026-08-11-round-four-audit.md` | Round four audit — **complete.** A55–A75, four critical (A55, A57, A58, A59). All four review streams landed. **A14 did not close** |
| 12 | `2026-08-13-closing-round-four.md` | Plan 1.12 — **complete, and the last audit-driven plan** (decided 2026-08-13). Closed A55, A56, A57, A58, A59 and A70; the other fifteen findings are carried as issues |
| 13 | `../audits/2026-08-14-design-audit.md` | The design audit — **complete.** Not like rounds one to four: it asked whether the *design* delivers the claim, rather than whether the code matches the design. Four streams, and the split it produced — *"same goal in → same pipeline out"* holds, *"nothing was guessed silently"* did not, and Plan 1.14 closed it — is the most useful thing it returned. This row said **next, not yet run** for two days after it ran, which is the drift A71 and A72 are about |
| 14 | `2026-08-16-the-forge-phase-1.md` + `../specs/2026-08-16-the-forge.md` | **The forge, Phase 1 — DONE, 2026-08-17.** Twenty-three tasks, seven checkpoints, all executed. `mendel-forge` is the first impure package and calls no model: a source is read, facts are derived, and everything else is a typed hole a person fills. Five corrections to the plan are recorded in [`journal/2026-08-17-the-forge.md`](journal/2026-08-17-the-forge.md); the two that mattered are that the diagnostics ownership guard was blind to every non-`MD` prefix, and that a draft could report no holes while producing a contract with no input ports. **The fifth-door question is settled: there is no door 5** (2026-08-17) — invariant 14 tracks prompt-derived data on the build path, `docs/design/clinical-data-protection.md` §4.2's taint rule is the scope it always had, and the forge is offline authoring outside it. That journal entry's Phase 2 section is still the handoff for everything else. Deterministic scaffolding only; a model fills holes in Phase 2. Plan 2 was deleted rather than corrected (operator's decision, 2026-08-16) — both documents were verified against the Plan 1.12 tree, fourteen merged pull requests ago, and neither knew about `AiProvenance`, `MD0225`, `coded()`, `declares:` or the registry being a separate repository. `43b1ce0` is the last commit holding them, and [`journal/2026-08-17.md`](journal/2026-08-17.md) is the argument. The runtime-AI and prompt→goal halves of the old Plan 2 are separate subsystems needing their own specs |
| 15 | [`2026-08-17-forge-phase-2.md`](plans/2026-08-17-forge-phase-2.md) + [`../specs/2026-08-17-forge-phase-2.md`](specs/2026-08-17-forge-phase-2.md) | **DONE, 2026-08-17.** Fourteen tasks, all executed; `make verify` green. Six corrections are recorded in [`journal/2026-08-17-forge-phase-2.md`](journal/2026-08-17-forge-phase-2.md), and the two that mattered were found by running the loop by hand with every test green: every model-access failure reached the user as a traceback, and a model id with no provider prefix escaped as a third-party exception. **The fifth-door question is answered — there is no door 5**, and `DOORS` and `tests/test_egress.py` did not change, which is the evidence rather than the claim. One decision moved during execution: the surface is `generate(shape)` with closed choice as a helper, not closed choice as the primitive, because the rule drafter runs next and does not fit a list of options. A model fills holes the deterministic scaffold left open. Four decisions taken 2026-08-17, before any plan: candidate-bearing holes only by default with prose holes behind an explicit flag; a model fill lands as an **answer** marked model-filled, since `Filler.MODEL` and the model id in `FilledValue.by` already reach `Provenance.drafted_by` and no schema changes; `ModuleSpec` gains **line numbers first**, so `Excerpt.text` quotes the line rather than naming the file — which buys conformance diagnostics the same thing; and a new **`mendel-ai` holds transport only** — LiteLLM client, config, retries — with no `AmbiguityResolver`, because that seam has no spec and invariant 9's replay requirement makes it unguessable. `sealed` makes no forge model call. The golden scaffold test stays pinned to `NoFiller` |
| 16 | [`2026-08-18-the-shared-question.md`](plans/2026-08-18-the-shared-question.md) + [`../specs/2026-08-18-the-shared-question.md`](specs/2026-08-18-the-shared-question.md) | **Plan 2.5 — DONE, 2026-08-18.** Eight tasks. The forge and the build path each had their own vocabulary for *a question a reviewer must answer*: `Hole`/`Ambiguity`, `FilledValue`/`Resolution`, `Filler`/`ValueSource`. One `Question`, one `Answer` and one provenance enum now, in a new `comeni_core/review/`. **What is deliberately NOT unified is the blocking** — a hole blocks and an ambiguity ships flagged, and that difference stays in the containers and the ports because `HoleFiller.fill()` may return `None` and `AmbiguityResolver.resolve()` may not. Putting it on the types would trade a structural guarantee for a runtime check, which is the mistake invariant 1 records. **The egress guard went red on its own** at the re-base commit and green once door 2's widening was decided — that sequence is the evidence the decision was made rather than skipped, and door 2 now carries `what`, `why_open`, `closed` and `evidence` because the forge measured 69% → 88% on exactly those. **`pipeline.yml` is byte-identical and the emitted digests did not move**, proven by building the same goal on both branches rather than asserted. Not a name collision with the *old* Plan 2.5: `CLAUDE.md` records that Plan 1.7 carried that name until 2026-08-05, and the date distinguishes them |
| 17 | [`2026-08-18-plan-3a-phase-0.md`](plans/2026-08-18-plan-3a-phase-0.md) + [`../specs/2026-08-18-the-interface.md`](specs/2026-08-18-the-interface.md) | **Plan 3 is A (forge) · B (landing) · C (Mendel)**, named 2026-08-18. **3A phases 0–4 are DONE, 2026-08-18** — 184 of 184 steps, on branch `plan-3-slice-1`, pushed and unmerged. Rows 17c–17f are the later phases; [`journal/2026-08-18-the-day.md`](journal/2026-08-18-the-day.md) is the handoff. Phase 0 was the foundation, ending on **one real action working end to end** rather than on a shell that compiles. It supersedes `2026-08-18-plan-3-slice-1.md`, which built a working backend and a frontend with **zero event handlers** — the slicing was wrong, and the checkpoint that reported it as "the queue on screen" without saying nothing worked was worse. Every phase now states what you cannot do at the end of it |
| 17c | [`2026-08-18-plan-3a-phase-1.md`](plans/2026-08-18-plan-3a-phase-1.md) | **DONE, 2026-08-18.** The queue: sort, filter, group, `J`/`K`/`↵`, and a `queue_visit` table for *changed since my last visit* — the second table, and the one-table guard was widened rather than loosened. Its Task 1 fixed a defect phase 0 shipped: `aggregate` sorted on `Band`, a `StrEnum`, so bands compared **alphabetically** and the queue put cosmetic above routing. Nothing asserted an order across bands |
| 17d | [`2026-08-18-plan-3a-phase-2.md`](plans/2026-08-18-plan-3a-phase-2.md) + [`../specs/2026-08-18-answering.md`](specs/2026-08-18-answering.md) | **DONE, 2026-08-18.** Answering: prose context, collapsed evidence, the `MODEL` marker, `answer-all` (best-effort, every refusal reported), the registry lookup panel, and *"nothing here fits"* — phase 2 **creates** a proposal, phase 3 decides it. The partial batch was exercised deliberately rather than hoped for |
| 17e | [`2026-08-18-plan-3a-phase-3.md`](plans/2026-08-18-plan-3a-phase-3.md) + [`../specs/2026-08-18-deciding-proposals.md`](specs/2026-08-18-deciding-proposals.md) | **DONE, 2026-08-18.** Approve, rename or reject. The vocabulary file is written at **land** time in one commit with the contract — *one review, not two* — and `verify` checks against the vocabulary the draft *would create*. Landing was actually run, into a throwaway clone |
| 17f | [`2026-08-18-plan-3a-phase-4.md`](plans/2026-08-18-plan-3a-phase-4.md) + [`../specs/2026-08-18-browsing-contracts.md`](specs/2026-08-18-browsing-contracts.md) | **DONE, 2026-08-18.** The contracts list and the module page, read-only by construction. Its audit corrected the spec: **`unverifiable` is about a missing source adapter, not a missing module file** — the two `comeni/` contracts are unverifiable *and* readable. **Phase 5 is done — row 17g; phase 6 is next** |
| 17g | [`2026-08-19-plan-3a-phase-5.md`](plans/2026-08-19-plan-3a-phase-5.md) + [`../specs/2026-08-19-resolving-drift.md`](specs/2026-08-19-resolving-drift.md) | **DONE, 2026-08-19** — 43 of 43 steps. Drift: what moved, what it means, and taking the source's value as one commit. Two checkers already ask the question and overlap on two fields, so the coverage is declared rather than merged; the verdict is a fold over a total classification of `ModuleContract`'s fields, so adding a field or a diagnostic fails a test rather than falling silently out of the answer; and accepting patches **one line**, because a contract's comments are its reasoning. Its audit found ten things before a line was written — including that `ops.update` re-opens every hole, which is why accepting is a patch and not a re-draft, and that this registry has **zero drift**, so the phase ships a recipe for manufacturing one rather than screens nobody has seen |
| 17h | [`2026-08-19-plan-3a-phase-6.md`](plans/2026-08-19-plan-3a-phase-6.md) + [`../specs/2026-08-19-sources-and-drafting.md`](specs/2026-08-19-sources-and-drafting.md) | **DONE, 2026-08-19** — 28 of 28 steps. Sources and drafting: what each source can read, what has been done with each tool, and starting a draft without a terminal. It **designs** a screen `forge-review.md` §9 lists as undesigned. Its audit found four things, and two are defects older than the phase: the forge's mounted transport takes filesystem paths from an unauthenticated request and is **deleted, not just unmounted** — zero consumers, an optional extra nothing installs, and a guide section telling operators to mount it; and `Workspace.save` has overwritten a draft silently since forge phase 1. A fifth was caught in the plan's own test — a boundary check that iterated `app.routes` found **zero** request bodies and would have passed vacuously |
| 17i | [`2026-08-19-plan-3a-phase-7.md`](plans/2026-08-19-plan-3a-phase-7.md) + [`../specs/2026-08-19-responsiveness.md`](specs/2026-08-19-responsiveness.md) + [`../audits/2026-08-19-performance-audit.md`](audits/2026-08-19-performance-audit.md) | **DONE, 2026-08-19** — 28 of 28 steps. Phase 7, and it did not exist until an audit said it should. Every registry-touching screen cost ~250ms warm against a stated budget of half a second; `layers.load` is 244ms of it, because the layer is walked five times — once per `DeclaredKind` — and libyaml is installed and unused. 86% of the test suite and 83% of `mendel build` are YAML parsing. The pure packages get speed and **no cache** (invariant 10, and the test suite is the beneficiary a cache serves worst); only `mendel-api` caches. The performance property is guarded by **counting, never timing** |
| 17j | [`2026-08-19-plan-3a-phase-8.md`](plans/2026-08-19-plan-3a-phase-8.md) + [`../specs/2026-08-19-compose-and-prod.md`](specs/2026-08-19-compose-and-prod.md) | **DONE, 2026-08-19** — 32 of 32 steps. Phase 8, and **it ends 3A**. `make dev` brings the whole thing up; prod builds the SPA and puts nginx in front; the nightly source check finally has a worker to run on. **Phase 6's guard decided the nginx question** — serving static files from FastAPI needs a mount, which that guard refuses. Two defects surfaced by asking what a container does: accepting a drift crashes when the registry is not a checkout, and the obvious fix (`.git` exists?) is wrong for a bind-mounted submodule, whose pointer resolves to nothing inside the container |
| 18a | [`2026-08-23-mendels-half-of-the-boundary.md`](plans/2026-08-23-mendels-half-of-the-boundary.md) + [`../docs/design/execution-boundary.md`](../docs/design/execution-boundary.md) | **DONE, 2026-08-23** — seven tasks, 52 of 52 steps, `make verify` green. **The gate half is proven and the executor half is not** — a distinction worth carrying: `nextflow config -profile awsbatch` resolving the executor is a far weaker claim than a pipeline running on Batch, the emitted profiles are deliberately incomplete fragments needing `-c site.config`, and that composition has never been executed once. §6 is what makes being wrong there cheap: the executor is inert config, so a bad profile is an `emit_config` edit rather than an artifact migration. The loop closes: draw, keep, gate, read the verdict — `lint` in **1.6s** and the route that starts one returns in **5ms**, because it writes a row and hands the work to ARQ. [`journal/2026-08-23-the-gate.md`](journal/2026-08-23-the-gate.md) is the handoff. **Three things were found by executing that neither audit pass caught**, and the pattern in them is the useful part: `MA` was already the AI adapters' and a header comment said otherwise (the strict loader caught the duplicate — in the same audit I verified one fact by reading the generator and took the other from a comment, and only one of those habits found a bug); **nothing in the browser had ever called the draft routes**, so 3E's create/save/keep and its 5s idle save have never run in production, which is a fact about the code the plan builds *on* rather than about the plan; and installing Nextflow is not one `curl` — `get.nextflow.io` ships a launcher that downloads the jar on first run, into a root-owned `NXF_HOME`, from a working directory it also needs to write. Two predictions were wrong in the good direction and are recorded so the next plan is calibrated: regenerating digests moved nothing, and the PREVIEW gate the self-review expected to fail passed. **STUB and TEST are deliberately not runnable in the container** — they need a Docker daemon, which means mounting the host socket, which is root-equivalent host access; §8 leaves it to Wiener. Originally: **PLANNED AND AUDITED, 2026-08-23.** Seven tasks, and **the audit found seventeen things before a line was written — three critical, and every critical came from the same root: the plan was written against what the code *resembles* rather than what it *is*.** A diagnostic prefix (`MD` is reserved for the three pure packages; an API code is `MA`), a `concern` value the doc generator refuses, and **every route path** — the build router carries `prefix="/pipeline"`, so drafts live at `/api/pipeline/drafts/{id}`. Each was plausible, wrong, and checkable in one command. A second pass then found four more, **three of them in the first pass's own repairs** — which is A157's lesson repeating at the same ratio. Seven tasks. Mendel's half of the execution boundary: `local`/`k8s`/`awsbatch` profiles in the emitted config, and a gate a person can run from the builder. **The spec is the reason it is small.** Nextflow already abstracts the three targets, so one emission task covers all of them; what the spec forbids is the executor entering the artifact, and the guard for that is a *signature* — `emit_config(pipeline)` takes one parameter, and a one-parameter signature cannot express a per-target emission. Writing the plan found two defects by asking what a container does, which is the method that found phase 8's two: **`MENDEL_DRAFT_ROOT` is set on the api and the worker with no volume behind it**, so `keep` writes into an ephemeral layer the worker cannot see, and **the runtime image has no Nextflow or Java** — invisible because `run_gate` degrades honestly rather than crashing. Deliberately not decided: mounting the host Docker socket into the worker, which `STUB` and `TEST` need and which is root-equivalent host access. LINT and PREVIEW need no Docker, so the loop closes without taking it. **Run management is Wiener's and is out of scope** — the spec's §8 names the risk as building it inside `mendel-api` because that is where the worker already is |
| 18b | [`../docs/design/wiener.md`](../docs/design/wiener.md) | **Wiener — DESIGN ONLY, no plan yet, written 2026-08-23.** Row 18a's journal named *Wiener slice 1* as what is next and `execution-boundary.md` §8 named the risk; this is Wiener designed before a route is written in the wrong place. **It was treated as unspecced** — every prior sentence about Wiener predates Mendel and was given no deference. Twelve decisions, and the load-bearing ones are: run state arrives as **typed `nf-weblog` events**, so the "logging rework" the brief anticipated turned out not to exist — the CLI flag is deprecated, the feature moved to config, and it is already an official plugin; **`wiener-core` is PURE and joins invariant 1**, because supervision splits into *deciding* and *doing* and only the doing touches the world; and **OpenTelemetry is the lens, never the record**, since sampling and expiry are what make a tracing store affordable and are exactly what run state cannot tolerate. Wiener's own claim mirrors invariant 10 — *same events in → same run state, same decisions, same AI brief* — which is why the model is **beside** the loop rather than in it, and why §5.1 forbids `wiener-core` from reading a clock. The AI is woken only by a failure **signature not seen before in this run**: 400 tasks and 41 failures cost three model calls. Two things fell out that were not asked for: extending invariant 1 to `wiener_core` makes it **structurally impossible** to put the OTLP exporter on the wrong side of the pure line, and §10 found that **a real run failure is the ground truth the rule drafter has never had** (issue #38) — so *the compiler gets better* routes through the forge queue rather than through a database, which invariant 2 would have forbidden. Six slices; **W1 is next and ends on a pipeline that ran**, not a shell that compiles |
| 18c | [`2026-08-23-wiener-w1-phases-0-2.md`](plans/2026-08-23-wiener-w1-phases-0-2.md) + [`../docs/design/wiener.md`](../docs/design/wiener.md) | **DONE, 2026-08-24** — twelve tasks, 84 of 84 steps, on `wiener-w1`, **merged into `main` on 2026-08-24**. **Three operator checkpoints**, and the first two were walked. It plans **phases 0–2 of W1 only** and says why: phase 3 is written against `RunState` and the projections these tasks create, and writing it now is writing against types that do not exist — the mistake this file records for Plan 2 and the original Plan 3. **Phase 0 is a library and a fixture**: `admit`, `fold`, `decide`, replaying the thirteen committed weblog events, with `wiener_core` added to invariant 1 and a new guard that scans the package for `datetime.now` — because §6.1's claim dies the first week a clock is read inside the fold. **Phase 1 ends on a pipeline that really ran**, which is the checkpoint that matters, since everything before it is testable without Nextflow and it is not. **Phase 2 ends on watching one**, and its checkpoint asks for the thing 76 green steps do not give: twenty minutes of use. Three guards are watched failing in phase 0 and three more in phase 1, including the four-table assertion and a column check that a `samplesheet` never lands in `run`. **One gap is named rather than left to be discovered**: §12.1 makes authentication a W1 requirement and this plan does not satisfy it — `submitted_by` is attribution, phases 0–2 are for one operator on a laptop, and the first deployment anyone else can reach needs the check first |
| 18d | [`2026-08-24-wiener-w1-phase-3.md`](plans/2026-08-24-wiener-w1-phase-3.md) + [`../specs/2026-08-24-telemetry-for-a-run.md`](specs/2026-08-24-telemetry-for-a-run.md) | **DONE, 2026-08-24** — fourteen tasks, 77 steps, on `wiener-w1`, **merged into `main` on 2026-08-24**. Thirteen were planned and the fourteenth is the pre-execution audit's (A184–A190). **Six checkpoints**, longer than usual on the operator's instruction, because three of its phases end on *a thing you look at* and that is what found nine defects in 3E which 76 green steps did not — and **those three are ticked `[~]`**: each was driven over HTTP and WebSocket and verified there, and nobody has opened a browser, so the checkpoint that was the reason for the shape is the one still owed. It finishes W1: the graph view, the telemetry, and four across-runs boards. **The research it rests on is done** — there is no batch-job convention (`opentelemetry-specification#1347`, open since 2021) and the CI/CD group fits, so `cicd.pipeline.*` and `cicd.pipeline.task.run.*` are adopted with `process.exit.code`, and exactly six facts keep `wiener.*` names because nothing standard has one. **Task 1 is a lift that does not quite reach**: `layout.of` takes a `PipelineIR` and Wiener has a `Pipeline`, so `dag-core` lays out a neutral graph and each half brings an adapter — with the builder's canvas diffed before and after as the safety net. **The boards live in the backend, not the SPA**, which is what keeps §9.4's *"this is not a second dashboard"* true |
| 18e | [`2026-08-24-wiener-w2.md`](plans/2026-08-24-wiener-w2.md) + [`../specs/2026-08-24-w2-reading-a-run.md`](specs/2026-08-24-w2-reading-a-run.md) + [`../../docs/design/w2-mockups/`](../docs/design/w2-mockups/) | **W2 — DONE, 2026-08-24**, on `wiener-w2`. Fifteen tasks, 87 of 87 steps, `make check` green at 1650 passed and the frontend gate green at 261. **Checkpoints 1 and 2 are walked and 3–6 are owed** — they need a browser, everything they judge is deployed. **Six defects were found by running it or by a guard and none by a test written to pass**, and one of them is worth more than the rest: a guard that *proved nothing and passed anyway*. The batching test asserted the events array had not changed synchronously after fifty messages — true with the fix and without it, because React batches renders itself; the frame count is what discriminates. A guard passing on the code it was written to reject is a green tick over an open hole, which is A67 from a new angle. Checkpoint 1 caught `steps_finished` counting a process whose every task FAILED (2 of 5 finished, zero successes); the drain test caught the console paging **once** at 200, invisible on every run anybody had because the largest is five tasks; and checking the built screens against the artboards found the overview **missing a whole column**. Two deviations are structural: `OverviewOut.rows` is a declared shape rather than the plan's `list[dict]`, and the walk is **`Walk.tsx` not `Rail.tsx`** — the plan asked for a filename that was already Plan 3C's side rail. Originally: PLANNED AND AUDITED, IN EXECUTION from 2026-08-24. Fifteen tasks, 87 steps, **seven checkpoints and six of them need a browser** — which is the debt row 18d names and does not pay: three of W1's checkpoints are ticked `[~]` because they were driven over HTTP and nobody opened a page. **The pre-execution audit ran against the code and the live database before a line was written** (A191–A200), and three of its nine findings changed the plan's shape rather than being carried as warnings. Two are worth knowing outside W2: **A191** — `run_task.attempts` is a JSON blob, so ordering by memory would load every document a run has, and three derived columns are what make the Tasks tab sortable; and **A200**, which *corrected the audit's own recommendation*. The artboards show a sample name on every task row and **nothing in the fold can name one** — a W1 guard, `test_the_fold_is_where_the_lab_strings_stop`, predicted this exact day in its docstring. The resolution is a `labels` column written by the **impure** projection from the raw payload, so `wiener-core` never sees a lab string and the guard is kept rather than argued down. Originally: **SPEC ONLY, no plan yet, written 2026-08-24.** The run console and dashboard properly. `wiener.md` §18 names five things and **two were already built** — the reconnect offset and the page-then-tail handoff both shipped in W1 — so what is left is the ending condition: *you can read a 400-task run without reading text*, which is a statement that the console cannot be the answer. It becomes a tab; the front door is one row per process the **artifact declares**, with all four of §9.3's comparisons on identical scales down each column. **The operator overrode the recommendation on that and was right**: exception-only hides a comparison behind an 80% threshold nobody sourced, which is a `why:`-less value wearing a UI costume. Nine artboards were drawn and corrected before the spec was written, which is the first time that order has been used here. Two defects came out of the design pass rather than a bug list: the events page loads once at `limit=200` and silently drops the rest, and `--hover` is referenced five times in `frontend/src/build/` and defined nowhere |
| 19 | [`plans/2026-08-30-plan-4-phase-0-the-shared-floor.md`](plans/2026-08-30-plan-4-phase-0-the-shared-floor.md) + the canvas | **Plan 4 Phase 0 — DONE, 2026-08-30**, on `worktree-plan-4-phase-0`. Five tasks, 27 of 27 steps; frontend gate green at 281 (from 267). Originally: **Plan 4 Phase 0 — PLANNED, 2026-08-30, not started.** The screens reworked, from the design canvas of 2026-08-29. Phase 0 is the floor all three screens stand on, built once so the Overview does not invent it and the Builder does not re-derive it a week later — one easing curve where there are currently **three** (`--t` is `.4,0,.2,1`, `Home.tsx` hard-codes `.2,.7,.3,1`, the canvas says `.32,.72,0,1`), five named movements, two content breakpoints, and **one answer to a mutation that failed**. That last is the walk's worst defect and the reason this is a phase rather than three tasks: *Keep* answered 500, the rail sat there still offering *Keep*, and the only way to learn the page's central action had failed was `docker logs`. The forge also leaves the navigation and **stays in the router** — hidden, not removed, by the operator's decision of 2026-08-30, with a test asserting every forge URL still resolves so *hidden* cannot rot into *broken*. **Two of its four pre-execution notes changed a task**: `tokens.test.ts` is already the generalised guard the canvas asks for (it walks every file and every `var()`, fallback chains included), so what is actually missing is the same guard one layer up — a motion **class** referencing a keyframe nobody wrote renders as *nothing moves*, which looks exactly like the reduced-motion path working; and `dashboard.md` §8 describes a different motion system in three sentences and is replaced rather than appended to |
| 20 | [`plans/2026-08-30-plan-4-phase-1-publishdir-and-results.md`](plans/2026-08-30-plan-4-phase-1-publishdir-and-results.md) | **Plan 4 Phase 1 — DONE, 2026-08-30**, same branch. Four tasks, 24 of 24 steps; `make verify` green at 1655. **It shipped publishing NOTHING with all five processes green** — `enabled: { … }` hands Nextflow a closure where it expects a value and it is never called; the stub gate said PASS, `nextflow config` printed the directive correctly, the log said nothing. `ls results/` is what found it, and no test in this repository could have had an opinion because the behaviour is Nextflow's. **The first fix was also wrong**: an expression `enabled` is read with the `process {` scope, so it sees a command-line `--outdir` and not a profile-set one — measured, profile+expression published nothing while `--outdir` published 41 files. Also caught: `outdir` is a null in the artifact, so `declared_holes` would have asked a person to type a filesystem path into the API. Originally: **Plan 4 Phase 1 — PLANNED, 2026-08-30, not started.** `publishDir`, and a run that can show you its outputs. Backend only, small, and **first** because it removes a caveat from both phases after it: the canvas names it three times independently, as *the third screen it has blocked* and *the cheapest large win available and the move the loop has never had*. A repository-wide grep finds `publishDir` in exactly one place — the list of directive names a setting is *allowed* to route to — so the vocabulary already permits it and nothing uses it. The design is the one `process.resourceLimits` already set: Mendel emits `params.outdir = null` and a `publishDir` that reads it, **exactly as it already emits `params.input = null`**, and Wiener's `site_config()` supplies the value — because *where the outputs go* is a fact about a site, and a number in the artifact would make `mendel emit` non-reproducible across deployments. `emit_config()` therefore keeps its **one-parameter signature**, which `execution-boundary.md` §6 made the guard against per-target emission. **`make verify` is required**, not `make check` — this touches `emit.py`, and nothing outside `test_counts.py` runs a tool |
| 21 | [`plans/2026-08-30-plan-4-phase-2-the-overview.md`](plans/2026-08-30-plan-4-phase-2-the-overview.md) + the canvas page 2 | **Plan 4 Phase 2 — DONE, 2026-08-30**, same branch. Seven tasks; `make check` green at 1686, frontend at 284. **The provenance bar cried wolf**: counting raw tier 4 reported five things needing a person where one did, because a hand-drawn graph records every step as `tier: 4, source: human` and `MD0220` says that is exactly what CLEARS a review. The service now reports five numbers and keeps `by_person` and `by_model` apart, because the 4→5 bump exists so an agent-assembled pipeline does not read as one a person drew. **The whole product moved to the Observatory palette** on the operator's decision — a large decision and a small diff, because not one component names a colour, which is now a guard. **The page was rendered and looked at in all three states**, which found three more defects a green suite could not: a `flow` marker that filled the whole remainder (saying everything not yet done is running now), a run titled by its id rather than its pipeline, and no elapsed time. Originally: **Plan 4 Phase 2 — PLANNED, 2026-08-30, not started.** `/` becomes the lab's work rather than the product's inventory, and **absence is absence**: the difference between the active and quiet artboards is not a different empty state, it is that the NOW band *does not exist*. Seven tasks. **Five pre-execution findings, three of which change a task, and two of which correct the design notes.** `pipeline_draft` **does** have an owner column (`who`, indexed, populated from `identity.default_author()`) where the annotation says it does not — no migration. **There is no `GET /drafts` at all**, which the canvas could not have seen because it was drawn against screens rather than routes, and it means the entire *by pipeline* half has no query behind it. `RunArtifact.pipeline_digest` is **declared and never written** — it is the join key this page needs twice, and the only candidate that keeps `execution-boundary.md` §9's rejection of a Mendel→Wiener API intact, so **the browser is the courier** here as it already is in `useSubmit.ts`. And Phase 0's hiding of the forge **empties half of `/api/attention`**, so *needs you* changes meaning from open forge questions to open tier-4 values in the lab's pipelines — which is what invariant 6 says must always be flagged, and what the artboard actually shows. Task 5 retires the constraint that says this page may not exist: `forge-review.md` §3 records an Overview page **designed and cut** and two tests hold it, and the operator has ruled it dead — so they are **restated to a narrower real rule** (pipelines and runs, never a contract, a question subject or a drift row) rather than worked around or left failing. Deliberately not built: *Changed underneath you*, which needs `upgrade --dry-run` per pipeline on a worker schedule and is its own phase after the Runs screens |
| 22 | [`plans/2026-08-30-plan-4-phase-3a-the-builder-shell.md`](plans/2026-08-30-plan-4-phase-3a-the-builder-shell.md) + the canvas page 1 | **Plan 4 Phase 3a — DONE, 2026-08-30**, same branch. Eight tasks; four boxes deliberately unticked and one `[~]`, which is how 3b's inheritance stays visible. Draw → Keep → Gate → Run is one **Run** action and a status line; `Walk.tsx`, `Steps.tsx` and the duplicated column are deleted. **Three things had been written down correctly in comments and nothing checked any of them**: `useGraph`'s autosave had never fired because no caller ever passed `save` — which made the plan's own argument for one Run button a FALSE premise until this phase made it true; `useBuilder.settling` had existed since 3E as the stale-verdict marker and no surface rendered it; and `/build?draft=<id>` was ignored, so every link phase 2 put on the front door opened the canonical spine. **A comment is not a guard.** A `rename()` that would have SAVED AN EMPTY GRAPH — deleting the pipeline while appearing to relabel it — was caught while writing it, and no test would have caught it. `freeSpot` was correct and deleted anyway: it guessed a cell before `dag-core` had seen the new node, and the layout already places the whole graph without overlap. **Four more found by rendering it at two widths**: an empty pipeline reporting *100% settled without judgement*, `1 to decide` printed twice, a permanent grid, and stacked panels keeping their desktop widths because `Side` sets width as an inline style |
| 25 | [`plans/2026-08-30-plan-4-phase-5-the-runs-screens.md`](plans/2026-08-30-plan-4-phase-5-the-runs-screens.md) + the canvas page 5 | **Plan 4 Phase 5 — DONE, 2026-08-30**, same branch. Four of five tasks ticked; **task 5's browser pass is the one deliberately open box**, and it covers all of Plan 4 rather than this phase. The rule it enforced: **a derived curve must look derived**. Phase 4 labelled the honest curves; this is where the label survives a renderer, and the failure mode is one word in somebody else's library — `curveMonotoneX` turns an area-true/shape-false curve into a picture of measurements nobody took, with no effect on the data and no test to notice. `curve.ts` builds the path and refuses a bezier. **The exact curves are steps too, and not as a house style**: a reservation genuinely is a step function, and a line sloping between breakpoints draws an instant at which nothing was reserved. Each curve keeps its own y-axis and they share the x-axis, because `cpus`, `bytes` and `bytes/s` have no shared scale and **time** is the comparison the artboard's overlay was for. **A scan over prose needs a boundary between what the code SAYS and what it QUOTES** — the failure banner's no-cause guard first scanned the whole banner and fired on Nextflow's own `errorReport`, which reads *an oom-kill event was detected*. Quoting the record is what the banner is for; authoring that sentence is what it must never do, and a scan that could not tell them apart would have forced the panel to censor the record to stay green. **Second scan in two phases to fire on the thing it protects**, both found by running rather than reading. **Two numbers were fetched and drawn nowhere** — `by_pipeline` since phase 2 and `TaskOut.history` since phase 4 — because nothing fails when a correct number is simply not rendered. The board's comparison keeps a rule the endpoint cannot: **a delta needs a finished run**, since `-43% vs usual` under a live bar reads as *it was faster*. The failing task's ask now comes from the **attempt** rather than the per-process aggregate, which on an escalating task reported a ceiling no attempt was given. **One panel took the whole page down**: three fixtures fell through to a `RunState` for the new route and the envelope threw during render — there is no error boundary above it, so it now answers *nothing to draw* for a shape it does not recognise. The `who` filter is cut and the slot stays, per the operator; `Overview` was a pass-through alias for `Table` and is gone |
| 26 | [`plans/2026-08-30-plan-4-phase-6-the-builder-rebuilt.md`](plans/2026-08-30-plan-4-phase-6-the-builder-rebuilt.md) + the canvas page 1 | **Plan 4 Phase 6 — four tasks of six, 2026-08-31**, same branch. **It exists because the operator opened the pages**, which is the whole finding: phases 0–5 shipped with every suite green, and the fonts were never loaded, the arc field every artboard sits on was never built, and the builder's canvas flowed the wrong way. **The guards here are good at behaviour and blind to appearance.** The graph now runs **left to right** — reversing a considered Plan 3C decision whose own test file warned that *a sideways graph would pass every assertion here*, and it did: one test asserted `rank` ordering and passed on both directions. `dag-core` is one implementation for both canvases, so the runs graph turned with it. **A comment claiming a guard exists is worse than no comment**: `geometry.ts` said its constants were held to `layout.py`'s by a named test "rather than trusting the comment", no such test existed, and by the time anybody looked `NODE_W` was 232 in the browser and 172 in Python — second instance in three days. **A defect only looking could find**: adding a step made its sibling vanish, because `seed` pinned every node once-ever and *being drawn somewhere is not being put there*; the saved draft was correct throughout, so no API test could see it. Ports became clickable — an operator decision reversed in writing, since a drag-only port is a gesture with no discoverable affordance. Tasks 5 (settings on the node) and 6 (the hand-driven walk) are open, and an **unscheduled chrome pass** is owed: the title row, provenance bar, canvas/artifact toggle and right rail are still Plan 3C's |
| 27 | [`plans/2026-08-31-plan-5a-the-registry-and-the-modules.md`](plans/2026-08-31-plan-5a-the-registry-and-the-modules.md) + [`../specs/2026-08-31-the-registry-and-the-modules.md`](specs/2026-08-31-the-registry-and-the-modules.md) | **Plan 5 Part A — COMPLETE as of 2026-08-31**, all four phases, on `worktree-plan-4-phase-0`; the registry half is comeni-registry#5 and #6, both merged. **What the plan did not predict, in the order it bit:** a contract's module source stopped being *derivable* (a path join became a key lookup, and 154 tests moved with the signature); **the stated order was impossible** — neither half can merge first, and `ENGINE_REF` pinning a pushed commit is what breaks the cycle; the Dockerfile had been broken since A1 because `make check` builds no image; and `registry_lint`'s own `Finding` class was a third emission shape the diagnostics scan could not see, so six codes were declared and unreachable. Four phases. **`vendor/` moves into `comeni-registry`**, beside the contracts that bind it, **one self-isolated directory per tool**. §2's research argues the other way — nixpkgs, Homebrew, Bioconda, Galaxy, Terraform and crates.io all separate declarations from artifact bytes, and nf-core's *catalogue* vendors nothing — and **§2.2 is why that does not decide it: a Comeni registry is not an index, it is a LAYER, and a layer has to be self-sufficient.** The hole that settles it: **an overlay can declare a contract for an in-house process and has nowhere to put that process's code.** Three attack passes are folded into the tasks. The two that changed the design: **`declared_entries()` is an allowlist by file EXTENSION**, so `module/meta.yml` would be digested and `module/main.nf` would not — a `pipeline.yml` pinning a layer digest would pin the metadata and not the executable code, which is issue #46's defect in a new place; and **`mendel` is `mendel_compiler.cli:main` and `mendel-compiler` is pure**, so the vendor verb becomes its own impure package, `comeni-vendor`. Third pass, on an axis the others missed: **`--registry X` used to mean *parse this person's YAML* and now means *execute this person's Groovy*** — not a reason to reverse it, but signed tags stop being a nicety and become a prerequisite for publishing overlays |
| 28 | [`plans/2026-08-31-plan-5b-what-a-pipeline-takes.md`](plans/2026-08-31-plan-5b-what-a-pipeline-takes.md) + [`../specs/2026-08-31-what-a-pipeline-takes.md`](specs/2026-08-31-what-a-pipeline-takes.md) | **Plan 5 Part B — COMPLETE as of 2026-09-01**, all five phases, merged. Two journal entries cover it and a filename sort orders them backwards: read [`journal/2026-09-01-a-channel-gets-a-name.md`](journal/2026-09-01-a-channel-gets-a-name.md) (phases 1–3) then [`journal/2026-09-01-the-fan-out-and-the-sheet.md`](journal/2026-09-01-the-fan-out-and-the-sheet.md) (phases 4–5). **The fan-out defect below is FIXED and was reproduced first.** Five phases. *A channel's identity and its cardinality are properties of the TYPE, not of the pipeline* — `entry_channel` has the param name baked into its Groovy, `goal_of` deduplicates `have` by `type_id`, and `StepInput.channel` is a `TypeId`, so two `annotation.gtf` inputs are one `params.gtf` and a GTF cannot vary per sample. A `Channel` gains a name, a param and a **scope**, with exactly two scopes and a written refusal of a third; two sample-scoped channels are *reads with their respective annotations*, emitted as a samplesheet. `SCHEMA_VERSION` 5 → 6. **Phase 4 is a correctness phase, not a feature one**: the attack found that `ch_genome_index_star` is a **queue** channel, so `STAR_ALIGN` runs as many times as the shortest of its three inputs — **with 24 samples it runs once and 23 are silently dropped**, invisible because the stub profile globs one sample pair. Fan-in is unimplemented in the same place: `InputPort.cardinality` is declared, defaults to `"1"`, and never reaches the emitter, so MULTIQC would produce one report per sample. Three more from the passes: the spec **contradicted its own egress claim** and `input_form` became a closed enum with a generated sentence; **channel names were keyed on node ids, which are history-dependent**, so ordering moved to `dag-core`'s shape arithmetic; and **`BuiltPipeline` has no channels**, the seam between the two halves, so the canvas would have disagreed with the resolver again. **Phase 1 — outputs on the canvas, computed today and drawn nowhere, plus labels — is independent and can land alone** |
| 29 | [`plans/2026-09-01-plan-6-what-the-run-page-still-owes.md`](plans/2026-09-01-plan-6-what-the-run-page-still-owes.md) + `.design/RunView.dc.html` | **Plan 6 — COMPLETE as of 2026-09-01**, all three phases, on `main`. [`journal/2026-09-01-the-run-page-and-the-first-verb.md`](journal/2026-09-01-the-run-page-and-the-first-verb.md) is the handoff. **Its lesson is a method:** *reading finds wrong strings; it does not find wrong pictures.* Comparing the run page to its artboard by reading annotations and HTML found six real defects; putting both in one viewport (`.design/_compare.html`, committed for this reason) found four more in one screenshot, and those were the ones that made the page look wrong — a panel is **translucent** over the arc field rather than an opaque card, the envelope was drawing at **half width** because `width=100%` over a fixed viewBox fits the HEIGHT, band headers carried a chrome fill the artboards do not have, and the timeline was squashed to 40px for five lanes. Three guards caught the implementation and all three were right: the four-table guard refused `run_intent` until §7.1 argued for it in prose, the OpenAPI guard refused two unnamed routes, and `reported.test.ts` refused a `useMutation` that did not hand back its error. **Watching the recycled-pid guard fail terminated the test runner** — without the start-time check the test signalled `os.getpid()`, which is pytest. Originally:  Three things the run page's artboard draws and the rebuilt page does not have, each left out for a stated reason and each checked against the code here. **Four findings reshaped it before a task was written.** *Cancel is not a button — it is W4's whole door*: `wiener.md` §11 is a **closed verb vocabulary** of five, every one a typed `Intent` requiring *approval by a named human* and an audit line, and §11 calls it *the surface that deserves the hardest audit in Wiener* precisely because a reviewer then checks a list of verbs rather than a sanitiser. Cancel is the right verb to open with for §11's own reason — *the only one that needs no artifact* — which argues for building the machinery under the cheapest verb, never for skipping it. *The launcher cannot cancel anything*: `launcher.py:110` discards the `Popen` on the line it makes it and no `pid` column exists, so most of phase 1 is giving the launcher a memory — and a pid is not an identity, since pids are reused. *The pipeline name is not missing, it is dropped at the courier*: `PipelineDraft.name` has existed since Plan 3E and `Pipeline` and `Goal` have none, so this is a courier question and not a schema break — unless CLI-built artifacts should have names too, which is the fork phase 2 takes deliberately. And *the timeline is not blocked*: `page-5` lists it under **BLOCKED, FAKE, OR NOT YET PROJECTED** and that annotation is now out of date — `Attempt.start_ms` and `complete_ms` are already written into `run_task.attempts`, so nothing needs measuring or migrating and what is missing is a query path and a component, the same shape as the `tag` column. Deliberately **not** in scope: the other four verbs (a second verb in the same plan is how a vocabulary becomes a pile), a sample axis (refused twice in the artboards, and `launcher.py:120` will not hold a samplesheet in any table), and `submitted_by`, which `page-5` already calls decoration |
| 30 | — *no plan yet* | **NEXT, decided by the operator 2026-09-01, in this order.** **(a) End-to-end manual testing.** Plan 6's own journal is the argument: every defect that mattered that day was found by a person looking at a screen and none by a suite that stayed green throughout — and the loop has not been walked by hand since the bands landed. It is the same finding as 2026-08-23's *walking the builder* and 2026-08-29's *walking the loop*, which between them produced twenty-three defects after their plans reported done. **(b) Repo cleanup.** **(c) The forge rework** — deprecated since 2026-08-31, with `make forge-rework` listing everything Plan 5A touched or invalidated and [`../specs/2026-08-19-the-forge-redesigned.md`](specs/2026-08-19-the-forge-redesigned.md) standing as its spec. Design work on **Comeni Code** starts in parallel and is a separate repository — `CLAUDE.md` says do not build it here, and nothing in this repo should grow toward it |
| 24 | [`plans/2026-08-30-plan-4-phase-4-what-a-run-cost.md`](plans/2026-08-30-plan-4-phase-4-what-a-run-cost.md) + the canvas page 5 | **Plan 4 Phase 4 — DONE, 2026-08-30**, same branch. All four tasks, every box ticked. **No pixels**: three things the runs screens need and cannot compute for themselves. `wiener_core/series.py` is a fourth pure module enforcing one rule — a scalar becomes an honest curve or it does not, and which depends on **how it distributes over its window**. A **reservation** is constant across its window so summing it is exact, not synthetic; a **total** spread uniformly is area-true and shape-false, drawn stepped and labelled `derived`; a **peak** does not distribute at all, and summing `peak_rss_bytes` across live attempts describes an instant that never happened. So `Kind` is `exact | derived` and **there is no third member** — nowhere in the type to put a curve whose shape cannot be trusted, which is stronger than a comment asking nobody to try, and it is the tempting one: memory-over-time is the chart everybody asks for. **Boundaries, not bins** — `+delta`, `−delta`, sort, prefix-sum: exact at every breakpoint, and 5,000 tasks is 10,000 events rather than a scan per bin. `bin_ms` ships as a *suggestion*, sized off the run's own recorded span, or a 40-second stub run collapses to one point. **No clock** (invariant 1), and a running attempt keeps its reservation to the right edge — closing open intervals at a clock made the *exact* curve fall to zero there, the precise artefact the derived curve is hatched to avoid, arriving on the half that is supposed to be trustworthy. `GET /runs/{id}/series` reads `run_task.attempts` and **never** `state_of` — A191's rule that a board is a query. **The guard for that passed while proving nothing**: `runs.py` does `from …projection import state_of`, so patching `projection.state_of` binds past it — the gotcha `CLAUDE.md` names by hand and this repo's conftest explains in a comment. The revert *was* verified landed; landing a revert and watching a guard fail are **two different checks** and only the first had a habit behind it. `TaskOut.history` exposes the **36 → 48 → 72 GB** escalation that was in a JSON column and out of every reader's reach, with what each try **asked for** beside what it **touched** — and no verdict: `137` is glossed as `SIGKILL`, arithmetic, because *the OOM killer did it* is an inference a preemption and a `kill -9` share, and §18.1 says nothing explains a failure until W3. **A scan broad enough to fire on its own rationale** — the first version forbade `because` and caught the docstring explaining the design it protects |
| 23 | [`plans/2026-08-30-plan-4-phase-3b-the-builder-surfaces.md`](plans/2026-08-30-plan-4-phase-3b-the-builder-surfaces.md) + the canvas page 1 | **Plan 4 Phase 3b — DONE, 2026-08-30**, same branch. All seven tasks, every box ticked but Task 7's `[~]` — the four new surfaces have component tests and no browser pass. **The typed input sockets were deferred on a claim that was wrong and the operator caught it**: they were called the same class of work as the decorative arc field, and they are not — a socket says what the pipeline requires of you, from data already in the browser (an input is entry-fed when it is `met` and no wire targets it, a question about EDGES rather than types). Ninety lines, no `dag-core` change. What they replaced was worse than nothing: a wire stub off the left edge with a clipped label and no terminus. The browse overlay replaced the palette and took the canvas's drop handlers with it — half a gesture whose other half no longer exists. `Restored.test.tsx` was restated a THIRD time, because three of the four operator-restored things it protects were about that palette; the question each protected is still answered, the answers moved. Swap **computes** its consequences: choosing a candidate validates the graph as it WOULD be and diffs the verdict, so *would break MD0506 align.index* is a finding rather than a prediction, and nothing is applied until asked. The settings card gained the **premise** — what tier 3 is yellow for, and the card had been saying a rule matched without ever saying what it matched on. The rail's scroll loss was two nested scroll containers. **A guard that fired on correct code drew a boundary that had lived only in design documents**: the invariant-15 check caught `Submit.tsx` asking for a path, which is legitimate — the run sheet is Wiener's, and its values never enter the graph. An allowlist of one, named, with the reason. The picker is the spine and stands alone: click a port, get only what fits, ranked by `(surplus, -priority, id)` — the router's own key, imported rather than retyped. **The reason is arithmetic**: asking what feeds featureCounts' coordinate-sorted BAM returns exactly `SAMTOOLS_SORT · the only producer of alignment.bam[coordinate_sorted]`, which is the canvas's own sentence, computed. `PortView` gained `states`, because without them the question is *what produces a BAM* — three contracts — when the port asks for one. **I sorted the consuming direction backwards and my own docstring caught it**, by printing the real registry's answer and reading it. And the guard `useCompatibility.ts` had asked for in prose for a week — its header ends *"A test asserts the absence"* and **no test asserted the absence** — now exists: `norule.test.ts` refuses a signature parsed in the browser. A comment claiming a guard exists is worse than one that does not, because it stops the next person looking |
| 17o | [`../specs/2026-08-19-the-builder-is-a-builder.md`](specs/2026-08-19-the-builder-is-a-builder.md) + [`2026-08-23-the-builder-is-a-builder.md`](plans/2026-08-23-the-builder-is-a-builder.md) | **DONE, 2026-08-23 — Plan 3E, on `plan-3e-builder`.** Twelve tasks, `make verify` green. A hand-drawn graph now emits real Nextflow, differing from `mendel build` by exactly one include — the trimmer the resolver adds and the drawing omitted, which is what `compare` reports as `mendel-only`. Nine things the plan got wrong were found by guards during execution and are in [`journal/2026-08-23-the-builder-is-a-builder.md`](journal/2026-08-23-the-builder-is-a-builder.md); the pre-execution audit's own twelve are in the plan. **Then the operator used it and found nine more in twenty minutes** — including two real Plan 3C layout bugs (a wire landing 39px from its chevron; the canonical spine rendering with crossed wires) and a CI typecheck that had never checked anything. [`journal/2026-08-23-walking-the-builder.md`](journal/2026-08-23-walking-the-builder.md) is that round, and its lesson is the gap between *the plan is done* and *the thing works*. 3C shipped a **visualiser**: a goal goes in, the resolver searches, the canvas draws what it found, and nothing on it can be changed. The operator's reframing is that it has to be a **Galaxy-style builder** — you assemble, it checks you, and it *also* shows what the resolver would do so you can compare. The insight is that this is **the same knowledge from a different route**: `resolve()` searches for edges; a builder is handed edges and asked whether they hold, and every fact that check needs is already declared. It changes nothing about `Goal` — a builder edits a **pipeline**, and `pipeline.yml` is already the save file. Also carried: **the entire forge needs testing and general rework**. **The 2026-08-23 revision corrected three things the first draft got wrong** — `MD04xx` is already allocated to gates and emission so validation takes `MD0500`; `InputPort` carries three kinds of requirement, so a verdict reports at three levels rather than returning a boolean; and an empty `type_id` needed an explicit rule. It also found the fact the first draft missed: **all three things a builder lets you do already have a declared `human_override` slot**, shipped in Plan 1.10. Four decisions were taken — a model's choices write a sibling field rather than `human_override` (a `comeni-core` schema break), drafts are Postgres rows under an opaque id because the API may not accept a path, editing is client-side against a server-computed compatibility index, and `compare` is the agreed cut line if the estimate breaks |
| 17n | [`2026-08-19-plan-3c-the-builder.md`](plans/2026-08-19-plan-3c-the-builder.md) + [`../docs/design/dashboard.md`](../docs/design/dashboard.md) | **Plan 3C — DONE, 2026-08-19.** Nine phases, 52 of 52 steps, on `plan-3c-builder`. Nine phases, 52 steps, **three operator checkpoints** — after the shell, after the graph, and at the end. The checkpoints exist because 3D's interface was rejected three times, and the cause was that nobody had designed those screens and they got designed while being built. **3C does not have that problem**: `dashboard.md` §4–6 gives resize ranges in pixels, zoom clamps, corner radii and which settings groups open by default, and `dashboard.html` is 922 lines of working pan, zoom and orthogonal routing. So the instruction is *do not invent screens*. Phases 0 and 1 are the two prerequisites every earlier plan named: the orchestration seam — a **lift, not a rewrite**, since `resolve_verbs`' own comment says the core needs no disk — and **DAG layout in Python rather than the browser**, so the thing a person screenshots gets the same determinism `.nf` has. **Two parts of the design are deliberately out of scope and said so up front**: the *Ask Mendel* tab is door 1, and drag-to-connect is `dashboard.md` §9's own gap |
| 17m | [`2026-08-19-plan-3d-the-forge-redesigned.md`](plans/2026-08-19-plan-3d-the-forge-redesigned.md) | **Plan 3D — DONE, 2026-08-19.** All six phases, 56 of 56 steps, on `plan-3d-forge`, unmerged. Six phases, 56 steps, against `specs/2026-08-19-the-forge-redesigned.md`. **Phase 1 is the whole point and it is measured before it is written**: over the 30 ports of the 12 landed contracts, alphabetical ordering — what ships today — puts the right type first in **1 of 30 holes (3%)**; ranking by the port name, the tool's own name and the namespace of what the module consumes puts it first in **25 of 30 (83%)**, which clears the spec's 80% bar with pure arithmetic over declared data and no model. Phase 1 then writes the producer `OpenQuestion.suggested` never had. Phases 4 and 6 are **deliberately thinner** and say so: they are written against a `Board` type phase 3 creates, and this file's own row 17b records that writing later phases against types that do not exist is what killed Plan 2 and the original Plan 3. Phase 6 is the landing page, **last**. **Executed result: 1 of 30 → 25 of 30 (83%).** Three
things the execution found that the plan did not: `suggested = candidates[0]` — what the plan
*told* me to write — labelled four unfounded holes **Confirm** and offered the alphabet, which is
invariant 6's mistake in a different costume; the Sources/Contracts join is a **union**, not a
lookup, or a contract no source can discover vanishes; and one test went **vacuous rather than
red** when its route was deleted, because a greedy `{id:path}` swallows its own parent path |
| 17l | [`../specs/2026-08-19-the-forge-redesigned.md`](specs/2026-08-19-the-forge-redesigned.md) | **Plan 3D — SPEC ONLY, no plan yet, written 2026-08-19.** The operator's verdict on the forge was *really unintuitive* and *unusable*, and the spec was written after **walking the whole loop as a user** — `discover → draft → show → fill → verify` against the real thirteen vendored tools — rather than after reading the code. **It is not a looks problem.** `forge draft samtools/faidx` opens eleven holes, seven of them `type_id`, and each offers *the entire twenty-two-type vocabulary in alphabetical order*, so `genome.fasta` and `measurement.rrna_fraction` are equally prominent answers to what a port called `fa` emits. §1.5 is why the fix is small: `OpenQuestion.suggested` is already plumbed through the API, the queue ordering, the candidate highlight and the *Ask/Confirm* label, and **nothing in production assigns it** — every `suggested=` in the repo is in a test, so `QueueRow`'s Confirm branch is unreachable. §3 merges Sources and Contracts into one Tools page behind a status board answering *is everything okay*, with Queue as a band; §7 fixes the order as ranking → layout → help, and the landing page **last**, which is the mistake 3B made. Blocked on nothing; [#77](https://github.com/comeni-project/Comeni-Labs/issues/77) is why the catalogue total renders `—` rather than `13` |
| 17k | [`2026-08-19-plan-3b-landing.md`](plans/2026-08-19-plan-3b-landing.md) + [`../specs/2026-08-19-the-landing-page.md`](specs/2026-08-19-the-landing-page.md) | **Plan 3B — DONE, 2026-08-19** — 17 of 17 steps, on `plan-3b-landing`, branched from `plan-3-slice-1` and unmerged. `/` is a landing page rather than the redirect phase 0 left there, and `GET /api/attention` is the one endpoint behind it. **It had to answer an argument against its own existence**: `docs/design/forge-review.md` §3 records an Overview page *designed and cut* for answering the same question as the Queue, and §4 calls the Queue "the only home". The spec §1 answers it — a front door says *what is this* and *what does this hold*, which no destination answers — and the discipline that keeps it from becoming the page that was cut is that **it counts and links, and never renders an item**, held by a test rather than by intent. The signature element reuses `dashboard.md` §1 rather than inventing: certainty is drawn as stroke, so a visitor is taught to read the canvas before ever opening one, and `tokens.css` is byte-identical. **Reading the finished page found what nothing else could** — its title is Mendel and the nav had a greyed-out `Mendel` tab, so the front door read as *the product is not built*; the tab is now `Builder` |
| 17b | [`2026-08-18-plan-3-slice-1.md`](plans/2026-08-18-plan-3-slice-1.md) + [`../specs/2026-08-18-plan-3.md`](specs/2026-08-18-plan-3.md) | **Plan 3 — spec written 2026-08-18; SLICE 1 of six is planned, the rest are not.** Each slice produces working software on its own and gets its own plan written against the code the previous one lands — writing all six now would be writing five against code that does not exist, which is what killed Plan 2 and the original Plan 3. Slice 1 is the forge queue end to end, and it comes before the Mendel canvas deliberately: the forge has a backend and the builder does not. The interface is designed ([`docs/design/forge-review.md`](../docs/design/forge-review.md), ten screens) and the spec is written against the code that exists: the forge's HTTP transport is already a mountable app with eleven routes, and the build path has no equivalent because its orchestration lives inside an argparse verb. §4 lists what must exist underneath — DAG layout, a consumers index, which pipelines pin a contract, #69 — each naming the screen that needs it. Supersedes `2026-08-02-mendel-api-and-dashboard.md`, which — predates the types it references. **Gains the tier-4 ambiguity resolver and [#69](https://github.com/comeni-project/Comeni-Labs/issues/69)**, decided 2026-08-17: invariant 6 flags tier 4 even at high model confidence, so a model answer still needs a human, and the thing that makes tier 4 tractable is the review screen rather than the model. Resolver and screen are one feature. #69 — `AiProvenance.available` hardcoded to `[]`, which `MD0225` refuses the moment a resolver writes `source: model` — is its **first design task**, not an assumption |
| 18 | `../specs/2026-08-13-the-rule-drafter.md` | **The rule drafter — spec only, no plan yet. Moved BEHIND Plan 3 on 2026-08-18** (it had been moved *ahead* of it on 2026-08-17). The earlier argument optimised for queue size — nothing authors tier-3 rules, so every ambiguity falls to tier 4 by default. What the project is short of is **feedback**: nothing renders a tier-4 queue, so there is no way to see what the drafter should be aiming at. Its spec also names four hard prerequisites and issue #38's closing note says the drafting question and the measuring question are the same one, which makes it the highest-design-risk item on the board and the worst thing to schedule under time pressure. Originally moved ahead of Plan 3 on the argument that each step shrinks the next one's job: nothing currently authors tier-3 rules, so every ambiguity falls to tier 4 by default, and tier 3 is the differentiator. Its spec names four hard prerequisites and a central risk, and `CLAUDE.md` says read it before building any part of the forge. Issue #38's closing note is the hardest part — the drafting question and the measuring question are the same one, and a measurement has no `meta.yml` to be ground truth |

### Why that order

**Plans 1.5 and 1.6 come before Plan 1.7** because publishing a bundle built on an unverified
spine would push a wrong pipeline through the door with no undo. That earned itself twice: 1.5
found a spine that counted with the wrong strandedness, and 1.6 found three contracts naming
output channels that do not exist.

**Plan 1.7 comes before Plan 2** for two reasons, neither of which was written down until
2026-08-05 — the ordering was asserted by an index and believed for a day.

- *Plan 2 is stale and Plan 1.7 is not.* Plan 2 was written 2026-08-02, before most of the
  types it references existed; the table above says so. Plan 1.7 was written 2026-08-04
  against real code. Every plan in this repository has needed correction during execution —
  six steps in the measurements plan, five in 1.5, six in 1.6 — and the fresher plan is the
  cheaper one to run.
- *Plan 1.7 is pure and Plan 2 is not.* Lockfiles, replay and publish are `comeni-core` and
  `mendel-resolver` work with no new dependency. Plan 2 stands up `mendel-ai`, LiteLLM and
  model access, and opens three of the four egress doors at once. Growing the deterministic
  core while everything is still testable offline is the cheaper sequence.

**The argument against, recorded because it is real:** nothing in Plan 1.7 moves the v1
success criterion, whose one unmet clause is the plain-language prompt — that is Plan 2 Task
3. And `mendel publish` is the door with no undo, built for pipelines drawn from a registry
the forge has not filled yet. If v1 becomes the priority, this order is the thing to revisit.

**Plan 1.8 comes before Plan 2** for one reason, and it is a deadline rather than a preference.
Audit finding A8 is that a resolved routing decision never reaches the pipeline: `_choose`
picks by id order and the resolver is consulted afterwards, only to fill in a record. Plan 2
plugs a model into that exact port. Fixing it first means fixing a signature; fixing it second
means fixing it with a model in the loop and an adapter already written against the broken
shape. The other twelve findings are ordinary technical debt and would not, on their own,
justify inserting a plan here.

**The argument against Plan 1.8, recorded because it is real:** it moves the v1 criterion no
further than Plan 1.7 did, and thirteen fixes is a lot of new code to write immediately before
an audit said fresh code is where the sharpest defects live. The counter is that this is
exactly why round two exists, and why Plan 1.8 Task 12 sets it up rather than declaring victory.
**The exit criterion for that loop was decided on 2026-08-06: no critical findings survive.**
Not "an empty audit" — no audit in this repository has ever come back empty, and important and
minor findings are filed and carried rather than blocking Plan 2 indefinitely.

**Known overlap — resolved 2026-08-13 in favour of absorption**, see
`plans/2026-08-13-plan-2-corrections.md` §2 — deleted with row 14, and in `43b1ce0`: Plan 1.7
ran first, so Task 4 kept only the *persistence* half and feeds the shipped `ReplayResolver`. Original statement of the collision:
Plan 1.7 Task 5 builds `replay.py` in `mendel-resolver`
and Plan 2 Task 4 builds `ReplayingResolver` in `mendel-ai`. They are not the same — one
replays recorded decisions when a curated bundle is edited, the other caches model answers
across runs — but they are close enough that building both without noticing gives two ways to
do one thing. Whichever runs second should absorb the first rather than duplicate it.

### Superseded on 2026-08-18: the rule drafter now runs *after* Plan 3

**What changed and why.** The ordering below argues that each step shrinks the next one's job,
and on that argument the drafter runs before Plan 3. The operator reversed it on 2026-08-18, and
the reason is that the argument optimises for the wrong quantity: **queue size rather than
feedback.** Nothing in the repository renders a tier-4 queue, so nobody has ever looked at one —
and the drafter is the item with the least design clarity on the board (four hard prerequisites,
plus issue #38's note that the drafting question and the measuring question are the same one).
Building it blind, first, under a one-month MVP deadline, is the worst available sequencing.
Plan 2.5 was inserted ahead of Plan 3 in the same decision.

The argument below is kept rather than deleted, because it is still the right argument for the
half that did not change — the forge before either of them.

### Forge Phase 2, then the rule drafter, then Plan 3 — decided 2026-08-17, reversed 2026-08-18

**Each step shrinks the next one's job.** That is the whole argument, and it is why the tier-4
ambiguity resolver — the piece that looks most like "the AI work" — comes last rather than first.

- **The forge fills the registry.** A tier-3 rule cannot match against contracts that do not
  exist, and hand-authoring a registry was never the plan.
- **The rule drafter fills tier 3**, which is what tier 4 is the fallback *from*. Nothing
  currently authors a tier-3 rule, so today every ambiguity the ladder cannot settle lands at
  tier 4 by default. Building a model into tier 4 first would be optimising the fallback path
  while the primary one sits empty.
- **Plan 3 then builds the review queue against a queue as small as it is going to get**, and
  builds the resolver beside the screen that consumes its answer.

**Why the resolver belongs with the GUI and not in its own phase.** Invariant 6 says tier 4 is
always flagged, *even at high model confidence*. So a model answer does not remove the human — it
saves them composing a reply to a question they must still decide. The thing that makes tier 4
tractable is therefore the review screen, and a suggestion with nowhere to be reviewed is half a
feature that invariant 6 guarantees stays half. Today a person answers a tier-4 question by
editing `pipeline.yml` with an `override_reason`, which Plan 1.14 built because that person had
nowhere to say why (A77).

**The argument against, recorded because it is real:** #69 is a `comeni-core` artifact design
question, and putting it inside a frontend plan is how design questions get answered badly. It is
row 17's *first* task rather than an assumption for exactly that reason, and it is filed as an
issue rather than left in this table so it cannot be quietly skipped.

**The other argument against:** this order moves the v1 success criterion no further than the two
plans before it did. Its one unmet clause is the plain-language prompt — goal extraction, door 1
— which is a *third* AI subsystem, distinct from both the forge filler and the ambiguity
resolver, and it is scheduled nowhere. If v1 becomes the priority, that is the row to insert, and
this order is the thing to revisit.

### The loop's exit criterion was overridden on 2026-08-13

**The rule until now:** the fix-then-re-audit loop exits when *no critical finding survives a
fresh audit*. Decided 2026-08-06, and it is why A14 stayed open through four rounds and why
Plans 1.7, 1.8, 1.10 and 1.11 each recorded an argument for deferring Plan 2.

**The decision:** Plan 1.12 is the last audit-driven plan. Plan 2 follows it regardless of what
a round five would find. The operator's reason is that the MVP needs to exist.

**What that means concretely, so nothing here reads as an exit that did not happen:**

- **A14 does not close.** It is carried, not resolved, and round four's own measurement of it
  stands: roughly a fifth of individual guards have a recorded revert. The loop did not exit; it
  was stopped.
- **Fifteen round-four findings are carried** — A60–A69 and A73–A75 as GitHub issues, A71 and A72
  fixed in `CLAUDE.md` directly. None is critical. Two are worth knowing about before Plan 2
  touches the same code: **A62** (`model_construct` and assignment aliases walk past the
  construction guard) and **A68** (the totality guard compares 60% of its field names against
  themselves).
- **Round five's remaining scope was always thin.** Of the three things round four could not
  reach, two — the protection profiles and any impure sender — have no implementation to audit
  yet. The third, the `slow` Docker lane beyond one counts-matrix probe, is real and unaudited.

**The argument against, recorded because it is real and this is the fifth time an ordering
decision has needed one.** Round four found four critical findings, three of them in the guards
themselves, and it found them because cold reviewers attacked a surface they had not written.
Plan 1.12's own surface — two new diagnostics, a changed `resolve()` signature, a rewritten
egress guard, a widened purity scan — gets no such pass, and the audit history says new code is
where the sharpest defects live. Every round so far has found something in the previous round's
fixes.

**The counter, which is why the decision stands:** the v1 criterion's one unmet clause is the
plain-language prompt, which is Plan 2 Task 3, and four plans in a row have now deferred it. An
audit loop whose exit criterion is "the next audit finds nothing critical" has no guaranteed
exit at all — no audit in this repository has ever come back empty. At some point shipping is
the decision, and this is that point. What protects the work is that the fixes are guarded and
the residue is written down, not that another round happened.

### Plan 1.10 — the pipeline file

`specs/` holds nine specs written one per audit root, and one that is not:
**`2026-08-07-the-pipeline-file.md`**. The nine describe fixes to code that exists; that one
describes a design change — `pipeline.yml` as a single readable artifact replacing
`pipeline.ir.json`, `mendel.lock.yml` and `PublishBundle`'s on-disk form, with every setting
carrying its tier, its route to the tool, and its reason.

**It takes precedence over the code it cites**, by the operator's decision on 2026-08-07: the
roots are being implemented concurrently, so its citations will drift, and where they disagree
the spec wins. It may relocate a guard the roots install; it may never weaken one.

**It ran as Plan 1.10 — after Plan 1.9 and *before round three*,** decided 2026-08-07, and it
is complete as of 2026-08-09. **Round three is next**, and the argument below is why it audits
this shape rather than the one before it.

The reason is A14's own logic. Round three is another revert-and-watch sweep over the guards, and A14 is
that a guard never watched failing may be **inert rather than merely weak**. Plan 1.10 moves the surfaces
those guards watch: three artifacts become one, `emit()` loses three arguments, door 4's payload type
changes, sixteen diagnostics arrive. Auditing guards immediately before that is auditing guards that are
about to move — and the guard ledger's rows would attest to code that no longer exists, which is the
exact failure A14 names. Doing 1.10 first means round three audits the shape that will ship.

**The argument against, recorded because it is real and this is the third time:** A14 is critical and
open, the loop's exit criterion is that no critical finding survives, and this defers it again. Plans
1.7, 1.8 and 1.9 each had a version of this argument made against them. The counter is that round three's
value depends on auditing the final surface, not that 1.10 matters more than A14. It changes door 4's
payload type and the shape `replay.py` reads, both of which Plan 2 builds on; doing it second would
mean fixing them with a model already wired to the old shape, which is the same argument that put
Plan 1.8 before Plan 2. **The argument against, recorded because it is real:** it moves the v1
criterion no further than 1.7 or 1.8 did, and the criterion's one unmet clause is still the
plain-language prompt — Plan 2 Task 3. Three plans in a row have now deferred it.

It must not start before 1.9 finishes. Root D is rewriting `diff_ir` and this spec re-targets it;
1.10 landing first would mean rewriting a critical-finding fix underneath its own author.

### What round three inherits from 1.10

Written for whoever audits next, because this is a large new surface and the point of doing
1.10 first was that round three audits the shape that ships.

**New and never audited.** `pipeline.yml` and its round trip; `mendel emit`, `upgrade
--dry-run` and `publish` on a pipeline file; `Pipeline` as door 4's payload; sixteen new
diagnostics (`MD0108`, `MD0202`–`MD0203`, `MD0206`–`MD0216`); `Lockfile.from_pipeline` and
`diff_pipeline`; `ValueSource.HUMAN` and the `needs_review`/`overrides` split.

**Four guards moved or were rebuilt**, and each is worth re-reverting rather than trusting
this plan's own probes:

- `tests/test_egress.py` — its roots come from `DOORS` now, not from `vars(egress)`. The old
  form walked three doors out of four the moment the publication payload moved modules.
- `tests/test_construction.py` — the `Pipeline` allowlist exempts a **spelling**
  (`model_validate` in `pipeline_file.load`), never a file.
- `tests/test_audit_regressions.py` — both A27 tests were rewritten, because both had been
  passing for the wrong reason: they hung a smuggled value on a contract declaring
  `params: []`, so it was dropped before anything validated it.
- `tests/test_counts.py` — has ledger rows now, for the first time.

**Two things this plan found and did not fix**, deliberately, and both are honest candidates
for a finding rather than a fix:

- `MD0216` shipped **inert**: the refusal was written, `make verify` was green, and reverting
  it broke nothing because no test covered it. A guard was written afterwards. That is A14's
  finding happening on the same day as A14's ledger row, and it is recorded rather than
  quietly repaired because the lesson is that green is not evidence.
- `Resolution.source` can be set untruthfully by any resolver, including a future model
  adapter. It is declared vocabulary rather than a proof, on the same standing as `confidence`
  and `reason`. Whether that is acceptable once Plan 2 wires a model to that port is a
  question for round three, not an oversight.

**The guard ledger has five new sections** (Tasks 6–11) and `notes/audits/guard-ledger.md`
is where every probe is recorded, including the ones that found nothing.

### Mendel is the engine and the AI is its primary operator — decided 2026-08-14

**The decision:** everything built through Plan 1 is the engine. A human can drive it and the CLI
is built so they can, but the intended operator is the AI — it turns plain language into a goal,
drives the engine, and `pipeline.yml` is the **save file** it sets down and picks back up, tunes
and re-emits, rather than carrying a pipeline in its context. The full statement is
`docs/design/mendel.md` §1; this records that it was decided and why it needed deciding.

**Why it needed deciding.** The 2026-08-14 design audit's synthesis compared the artifact to a
chat window and asked whether a bench scientist could follow it. The operator's correction was
that this compares a substrate to a product: through Plan 1 there is no AI layer at all, so the
comparison was category-confused. The repository had been documenting both readings at once —
`CLAUDE.md` calls a pipeline "a shareable artifact… the file a reader opens", the artifact's own
header says "Read it; edit it" — without ever saying which one is primary. Two readings implying
different amounts of work is exactly the shape of thing this file exists to settle.

**It costs no invariant, and the check matters more than the conclusion.** An agent running
`mendel build`, editing `pipeline.yml` and running `mendel emit` is a *user of the CLI*, outside
the engine, exactly as a human at a terminal is. Invariant 3 constrains what **Mendel** calls, not
who calls Mendel; the three runtime AI points stay three and the four doors stay four. If a later
design does put a model inside the engine to tune an artifact, that is a fourth point and a new
door, and it must be argued on its own rather than inherited from this decision.

**What it changes: three audit findings get promoted.** Under a human-primary reading these are
legibility defects; under this one they are correctness defects, because the failure mode is a
machine's rather than a person's.

- **A104/A105/A77 — `why:` does not track its value.** A human who edits a value and leaves a
  stale reason beside it does it occasionally and may notice. An agent does it systematically and
  does not.
- **A106 — five of six values reaching the tools carry no `why:`.** A person reading a blank asks;
  **a model reading a blank fills it** from its own knowledge, with a deterministic pipeline
  standing behind the guess lending it credibility. That is the exact failure this design exists
  to prevent, arriving through the component meant to prevent it.
- **A130 — nothing distinguishes a model-authored `why.reason` from a human-authored one.** Under
  `guarded` and `sealed`, where attribution is required, that distinction is load-bearing. It was
  a medium finding; under this decision it is a gap in the protection profiles.

**The argument against, recorded because it is real.** "The AI is the primary operator" is a
claim about a component that does not exist yet — Plan 2 Task 3 is the only thing that will make
it true, and it has been deferred four times. Deciding the shape of an interface around a consumer
nobody has built is how the four-file bundle got designed, and `notes/plans/` is full of
plans written against types that turned out not to exist. There is a real risk that "what the
agent needs" is guessed here and guessed wrong.

**The counter, which is why the decision stands.** It does not add machinery for a hypothetical
consumer; it *raises the standard* on fields the artifact already has and the design audit already
found wanting, and every one of the promoted findings is a defect under the human reading too —
they only change severity. Nothing is built speculatively. And leaving it undecided was itself a
choice with a cost: the audit spent a stream's worth of judgement on a question the documents
could have answered.

### On the numbering

`1.5`, `1.6` and `1.7` are the deterministic core: no AI, no network, no new dependency.
Plan 2 is AI and the forge; Plan 3 is the API and dashboard. **Plan 1.7 was called "Plan 2.5"
until 2026-08-05** — that number recorded when it was written rather than when it runs, and
was read as the latter by everyone including its author. Journal entries dated on or before
2026-08-05 still say "Plan 2.5"; they are append-only and were correct on their date.

Plans 2 and 3 were written ahead of the code they build on, and say things that are no longer
true. That is the reason this directory is labelled the way it is.

### specs/ — the two that are not audit roots

`2026-08-07-the-pipeline-file.md` designed `pipeline.yml` and **shipped** as Plan 1.10.

`2026-08-13-the-rule-drafter.md` is the other, and it is **unscheduled on purpose**. Tier 3 is the
differentiator — the only tier that makes Mendel a resolver rather than a well-documented template
engine — and nothing currently produces tier-3 rules. Plan 2's forge drafts contracts, vocabulary
states and parameters; rule drafting is deferred past Plan 3 by that plan's own reasoning, because
it needs a corpus of real tier-4 flags. The spec records the design so the deferral does not also
lose it, and names four hard prerequisites and the central risk: a model drafting from literature
will produce citations that look right and sometimes are not, and **a rule with a fabricated
citation is strictly worse than no rule**.

## audits/

`2026-08-03-plan-1-audit.md` — an independent review that defeated all three
test-enforced invariants, using four lines of Python for one of them. All four defects are
closed, and the guards in `tests/` are the shape they are because of it. Worth reading
before trusting any guard in this repository.

`2026-08-06-plan-1-to-1.7-audit.md` — the same exercise over everything through Plan 1.7.
Thirteen findings, all reproduced by execution, **none fixed yet**. All three guards fell again,
by shapes they were never written against rather than by shapes they missed. Two findings are
in mechanisms Plan 1.7 shipped and are the reason to read it before starting Plan 2: a resolved
routing decision never reaches the pipeline (A8), and a layer digest does not cover the bytes
the registry loads (A9).
