# 2026-08-24 — W2, and a run you can read

**Read this first if you are picking the project up. This is the newest entry.**

**Three entries share the date `2026-08-24`** and a filename sort does not order them. They were
written in this order, and the last one is this file:

1. [`2026-08-24-wiener-w1.md`](2026-08-24-wiener-w1.md) — W1's handoff, still true
2. [`2026-08-24-the-courier-and-w2-designed.md`](2026-08-24-the-courier-and-w2-designed.md) — the
   courier, and W2 designed without a line of it being built
3. **this file** — W2 executed: fifteen tasks, 87 steps, on `wiener-w2`

## The screens were drawn before they were built, and the drawings are the reference

**[`docs/design/w2-mockups/`](../../design/w2-mockups/)** — nine artboards, drawn and
corrected by the operator *before* the spec was written, which is a new order here and it worked.
Published as a canvas at
<https://claude.ai/code/artifact/b36d76fb-0025-4a6a-9f10-400bcc10de10>, and the HTML is live, so
the hover states work in a browser.

**They are the authority where they and the code disagree**, and that is not theoretical: a
conformance pass on 2026-08-24 found the overview **missing a whole column** and six smaller
divergences. If you are changing a run screen, open the artboard for it first.

| artboard | screen |
|---|---|
| `Main.dc.html` | the overview — a run going well |
| `Expanded.dc.html` | a process row expanded to its tasks |
| `Failure.dc.html` | a run that failed — banner, then the overview |
| `Tasks.dc.html` | the Tasks tab, across the whole run |
| `Console.dc.html` | the console, filtered, no longer the front door |
| `Graph.dc.html` · `GraphMenu.dc.html` | the graph, and its menus |
| `Menus.dc.html` | the right-click vocabulary, §12.3 |
| `Walk.dc.html` | draw → keep → gate → run as one rail |

`python3 build.py` in that directory regenerates every artboard from the tokens. It is a design
tool and nothing in `packages/` or `frontend/` imports it (A196) — its hover CSS was re-typed by
hand into `frontend/src/tokens.css` rather than copied.

**They live only on `wiener-w2` and have never been on `main`.** They arrive there when
[#86](https://github.com/comeni-project/Comeni-Labs/pull/86) merges; until then, do not delete
the branch.

## Where things stand

- **W2 is built.** `/runs/{id}` opens on one row per process the artifact declares. The console
  is a tab.
- **Checkpoints 1 and 2 are walked; 3, 4, 5 and 6 are owed** — they need a browser and the
  operator is doing that pass. Everything they judge is deployed and reachable.
- **`make check` is green** (1650 passed) and the frontend gate is green (261 tests, `tsc` clean,
  lint 0 errors).

## What exists that did not this morning

`overview(state, declared)` in `wiener-core` replaces `stats()`: same §9.3 arithmetic, but every
declared process gets a row **before the run reaches it**, ordered by the artifact. That is what
lets it be the front door rather than a log — the table's length is known before the first event,
so it does not grow as the run discovers work.

`GET /api/runs/{id}/overview` and `/tasks` serve it. `run_task` gained four columns: three
derived (`peak_rss_bytes`, `realtime_ms`, `pct_cpu`) so the Tasks tab is an `ORDER BY` rather than
5,000 JSON documents, and `labels` — A200's resolution, written from the admitted event by the
*impure* projection, so `wiener-core` still never sees a lab string.

The screens: `Overview`, `TaskRow` with two callers and a test holding the count, `Tasks`
virtualised, `Failure`, `Menu`, and `Walk` — draw → keep → gate → run as one sequence.

**`--hover` now exists.** It was referenced five times in `frontend/src/build/` and defined
nowhere from Plan 3C until today, so five hover states were dead CSS — most of why the builder
felt inert. Writing the guard found two more: `--profiled` and `--settled` were names for tiers
that never existed in the palette.

## The defects, and where each came from

**Six were found by running it or by a guard, and none by a test written to pass.** The split is
the useful part.

| found by | what |
|---|---|
| **Checkpoint 1, on a real run** | `steps_finished` counted a process whose every task FAILED — `0d3a4e3d` read **2 of 5 finished with zero successes**. §5's one honest bar advancing on failure |
| **the drain test** | the console paged **once** at 200 and subscribed. Invisible on every run anybody had, because the largest is five tasks |
| **the drain test, again** | *mine*: the paging loop's `setEvents` updater closed over the loop's mutable `page`, and React runs updaters later. 437 events came back as 237 |
| **the gesture test** | the task row had no menu — the exact shape of bug *a menu on one page and nowhere else* describes |
| **rules-of-hooks lint** | a `useQuery` placed after an early return; five Graph tests failed with it |
| **checking against the artboards** | the overview was **missing a whole column**, and six smaller divergences |

**One guard proved nothing and passed anyway**, which is worth more than the defects. The
batching test asserted the events array had not changed synchronously after fifty messages —
true with the fix *and* without it, because React batches renders on its own. A guard that passes
on the code it was written to reject is a green tick over an open hole. The frame count is what
discriminates. That is A67's lesson from a new angle: a guard must be watched failing **against
the specific defect**, not merely watched failing.

## What a fresh reader gets wrong

- **"The plan was followed exactly."** Fourteen deviations are in its execution table. Two are
  structural: `OverviewOut.rows` is a declared shape rather than the plan's `list[dict]`, because
  an untyped generated client is the drift it exists to prevent; and the walk is **`Walk.tsx`,
  not `Rail.tsx`**, because `Rail.tsx` already existed as Plan 3C's side rail. The plan asked for
  a name that was taken.
- **"`steps_finished` is monotonic."** It is not and cannot be — Nextflow discovers tasks as
  channels emit, so a step with three tasks done is finished until a fourth is submitted. Pinned
  by a test, because the monotonic alternative is the one that looks better and is false.
- **"The old runs have the new numbers."** They do not. The migration back-fills nothing on
  purpose: the columns derive from `run_event`, so an old run re-projects if anybody needs it.
  Every run before today reports `null` for the three derived columns and for `tag`.
- **"The resource line is broken on the failed run."** It is absent, which is the rule working:
  half a comparison invites the reader to supply a ceiling they do not have.
- **"W2 is all Wiener."** Task 14 is a Mendel screen — §13's scope creep with a stated reason.
  It is built and still cuttable in one commit.

## What is next — the browser pass, and it is the whole of what is owed

**Nothing else blocks W2.** Every checkpoint below is deployed and reachable right now; the
stack is up from this worktree (`docker compose ps` to confirm). They are recorded as **owed
rather than ticked**, because ticking a checkpoint nobody watched is a claim and not a record.

**Walk them in this order** — each one sets up the next.

### ✋ Checkpoint 3 — a run you can read

<http://localhost/runs/4499bf9e669553f677f5c20964b13e03> — the nf-core test data, four
processes, finished 4 of 4. STAR_ALIGN peaked at 1.27 GB against a 31 GiB cap.

- Can you tell what happened **without scrolling and without reading a sentence**?
- Does a column of bars let you name the memory hog in under two seconds?
- Does any bar go backwards, or claim a total that later grows?
- Expand STAR_ALIGN. Does the task table under it answer *what did this process do*?
- **Twenty minutes of use.** Write down everything that annoys you — that is what found nine
  defects in 3E which 76 green steps did not.

### ✋ Checkpoint 4 — a failed run says where it failed

<http://localhost/runs/0d3a4e3d359a7615d83ec99042dbbb6b> — STAR_GENOMEGENERATE, exit 127, with
Nextflow's own `errorReport`.

- Is the failing task named **above the fold**, without a click?
- Are its siblings visible for comparison?
- Does the report read as evidence rather than as a wall?
- Would you know what to do next? (You should not be *told* — that is W3.)

**Two things to expect rather than report.** The **resource line is absent**, because that run
predates the derived columns and neither half of the memory comparison was recorded — that is
*only when both halves are known* working. And the task identifies itself by **id rather than
tag**, because `labels` did not exist when it was projected. Both are A200's fallbacks, and
seeing them is the point.

### ✋ Checkpoint 5 — the gesture works everywhere it is offered

Right-click, on either run page: **a board row** (`/runs`), **a process row**, **a task row**
inside an expanded process, **a console line**, **a graph node**, and **the graph background**.

Then the two that must NOT open a menu:

- **a text selection** — select some of the failure report and right-click it. You must get
  **Chrome's** menu. Overriding right-click everywhere steals *Copy* from people, which is a
  worse bug than having no menu at all.
- the failure report block itself.

Then: canvas menu → **Copy the graph as SVG**, paste it into a file, open it. The token values
are resolved into the copy, so it should render outside the app rather than arriving with every
stroke unset.

**Keyboard**: tab to a row and press **Shift+F10**. Escape closes it and focus returns to the row.

**W4's verbs are listed and dimmed on purpose** — a menu that grows two new items in six months
is worse than one that always had the shape. Two task-row items read `not served` rather than
`W4`, and that distinction is deliberate: the work directory and command line are lab strings
A200 decided stay in `run_event.payload`, so nothing intends to add them.

### ✋ Checkpoint 6 — one journey, and the one you can cut

<http://localhost/build> — the walk: Draw → Keep → Gate → Run.

- At every moment, is it clear what the next step is and why the others are not available?
- **Change the graph after keeping it.** Does the rail go *backwards*, in words, without a hover?
- Does the run open in a state you can read?

**This is the Mendel screen inside a Wiener slice** — §13's scope creep with a stated reason.
It is built, and cutting it is one `git revert` of the commit that added `Walk.tsx`. The gate and
run *panels* were relocated into it, not rewritten, so nothing is lost by reverting.

### And Checkpoint 2, if you want it

`--hover` is proven to resolve in the shipped CSS, so what is left there is a judgement I cannot
make: is 5% the right tint, and does `--e3` on the floating panels read as lift or as fog?

## After the pass

**If the checkpoints hold**: tick them in the plan, and merge the PR.

**If they do not**: the fixes belong on this branch before it merges, which is the point of
walking them before rather than after. 3E's lesson is that twenty minutes of use finds what 76
green steps do not, and this slice has 87.

**Still true and unchanged:** the forge needs testing and rework, and W3 is the AI, after W2.
