# 2026-08-13, evening

**Everything is merged. `main` is at `440336d`, 608 fast tests green, `ruff check` clean.** PRs
[#23](https://github.com/comeni-project/Comeni-Labs/pull/23) and
[#37](https://github.com/comeni-project/Comeni-Labs/pull/37) both landed as merge commits, matching
the repo's convention and keeping the per-task history — each of Plan 1.12's commits records what
it corrected, and squashing would have thrown that away.

**Read this entry before resuming. It exists because a session ended mid-audit.**

## What landed after Plan 1.12

- **`docs/internal/specs/2026-08-13-the-rule-drafter.md`** — where tier-3 rules come from. Nothing
  currently produces them: Plan 2's forge drafts contracts, vocabulary states and parameters, and
  rule drafting is deferred past Plan 3 by that plan's own reasoning. The deferral is right;
  losing the design with it was not. Four hard prerequisites, and the risk stated first because a
  plan must open on it — **a rule with a fabricated citation is strictly worse than no rule.**
- **`docs/design/rule-tables-and-port-logic.md` §13** — what the rule format cannot express, and
  what each repair costs. Three limits, each with a canonical rule behind it. Issues
  [#38](https://github.com/comeni-project/Comeni-Labs/issues/38) (nothing drafts measurements) and
  [#39](https://github.com/comeni-project/Comeni-Labs/issues/39) (the format limits).
- **`docs/internal/plans/2026-08-13-plan-2-corrections.md`** — the rewrite `docs/internal/README.md`
  has demanded since 2026-08-05. Six corrections verified against the tree, and two things
  expected to be stale that are **not**.

**One correction to something said earlier in that session, recorded because it would have cost
somebody a day:** A56 did **not** change the `AmbiguityResolver` port. It changed the module-level
`resolve()` in `resolve.py`. The port is still `resolve(self, ambiguity) -> Resolution`, exactly as
Plan 2 Task 5 declares it. The real A56 consequence is semantic and easier to miss —
`DecisionStore.override()` writes a human override that is **silently ignored** unless the record
reaches `resolve(prior=…)`. It fails safe (over-flagging), but it fails.

## The design audit — stream 2 started and did not finish

Stream 2 of `docs/internal/audits/2026-08-14-design-audit-brief.md` was dispatched as a cold
reviewer and **died on a 529 Overloaded**, which is a server-side error and says nothing about the
work. **No findings were recorded** — its reasoning lived in its context and went with it. Do not
assume anything about what it concluded.

**Its artifacts survived**, and they are the expensive part:

```
.claude/worktrees/agent-a10b5c48d95b529ce/audit-tmp/     (~3.1 MB, untracked)
  nonf/     the non-nf-core contract experiment — question 1, the highest-value one.
            Its own layer with vocabularies for non-nf-core types (report.txt, table.tsv,
            summary.tsv), and FOUR build directories that produced main.nf + nextflow.config.
            build2/ carries a .nextflow.log, so Nextflow actually ran.
  md0211/   a diagnostic probe, with its own .nextflow.log
  coll/ conf/ lie/ elsewhere/   four further probes
```

**That worktree is untracked scratch and nothing protects it.** If it matters, copy
`audit-tmp/nonf/layer/` somewhere durable before pruning worktrees — the hand-written non-nf-core
contract and its vocabularies are small, reusable, and the slowest part to recreate.

### Resuming it

1. **Try resuming the agent first** — id `a10b5c48d95b529ce`. If it still resolves, its context
   comes back and nothing is repeated. Its last recorded next step was *"how the compiler joins two
   channels into one process input."*
2. **If it does not resolve, relaunch cold** from the brief's stream 2 section, and **point it at
   the surviving `audit-tmp/`** so it does not rebuild what is already on disk. Its number block is
   **A90–A103** and belongs to that stream alone.
3. Either way, the brief's rule stands: **re-verify every claim first-hand before recording it.**
   A finding the synthesiser has not reproduced is PLAUSIBLE, not a result.

Round four had two streams hit limits and relaunch, and both completed. This is the same shape.

## What is next

**Streams 1, 3 and 4 of the design audit have not run at all.** Stream 2 was run first
deliberately: whether a contract can express a module that is not nf-core-shaped bounds what the
forge can ever ingest, and that answer is wanted before the drafter is designed rather than after.

Then **Plan 2**, corrections first. The forge slice — Tasks 1A, 1, 8, 9, 10, 11 — is unblocked by
#38 and #39, because it drafts contracts, states and parameters rather than rules. **Task 3**
(prompt → `Goal`) is the separate branch off the same 1A+1 foundation, and it is the one unmet
clause of the v1 success criterion.

Two carried findings to read before Plan 2 touches the same code:
[#26 (A62)](https://github.com/comeni-project/Comeni-Labs/issues/26) and
[#32 (A68)](https://github.com/comeni-project/Comeni-Labs/issues/32).
