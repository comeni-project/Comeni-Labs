have:
  - type_id: fastq.reads
  - type_id: annotation.gtf
  - type_id: genome.fasta
want:
  - counts.matrix
constraints:
  required_states:
    counts.matrix: [gene_level]
profile:
  read_length: 150
  strandedness: reverse
  n_samples: 12
  paired: true
