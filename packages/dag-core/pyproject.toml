[project]
name = "dag-core"
version = "0.1.0"
requires-python = ">=3.12"
description = "Laying out a directed graph. Shared by both halves, pure."
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/dag_core"]
