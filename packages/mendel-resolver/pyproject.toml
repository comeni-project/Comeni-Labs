[project]
name = "mendel-resolver"
version = "0.1.0"
requires-python = ">=3.12"
description = "Four-tier deterministic resolution: backward-chaining router and validated tier-3 rule tables"
license = "Apache-2.0"
license-files = ["LICENSE"]
readme = "README.md"
authors = [{name = "Rafael Correia"}]
keywords = ["bioinformatics", "nextflow", "workflow", "reproducibility", "pipeline"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Bio-Informatics",
    "Typing :: Typed",
]

# A lower bound and no cap. The bound is what makes an independent version mean something:
# without it a released wheel accepts any comeni-core, including one predating a type it
# imports. A cap would be a promise to bump in lockstep, which is what independent versioning
# exists to avoid — see docs/guides/releasing.md.
dependencies = ["comeni-core>=0.1.0", "pydantic>=2.9", "pyyaml>=6.0"]

[project.urls]
Homepage = "https://github.com/comeni-project/Comeni-Labs"
Documentation = "https://github.com/comeni-project/Comeni-Labs/tree/main/docs"
Repository = "https://github.com/comeni-project/Comeni-Labs"
Issues = "https://github.com/comeni-project/Comeni-Labs/issues"
Changelog = "https://github.com/comeni-project/Comeni-Labs/blob/main/CHANGELOG.md"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mendel_resolver"]
